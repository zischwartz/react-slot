To get started download or clone this repo and `cd` to it.


Then from within this directory run
```bash
npm install
npm start

```

And go to [http://localhost:3000/](http://localhost:3000/) in your browser and start editing the `src` directory!
